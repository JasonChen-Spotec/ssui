import React from 'react';
import Icon from '../components/Icon';
import User from './components/base/User';

const UserOutlined = props => (
  <Icon {...props} component={User} />
);

export default UserOutlined
;
