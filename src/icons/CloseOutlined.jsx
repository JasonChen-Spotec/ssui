import React from 'react';
import Icon from '../components/Icon';
import Close from './components/base/Close';

const CloseOutlined = props => (
  <Icon {...props} component={Close} />
);

export default CloseOutlined
;
