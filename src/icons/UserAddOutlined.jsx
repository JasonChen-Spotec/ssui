import React from 'react';
import Icon from '../components/Icon';
import UserAdd from './components/base/UserAdd';

const UserAddOutlined = props => (
  <Icon {...props} component={UserAdd} />
);

export default UserAddOutlined
;
