import React from 'react';
import Icon from '../components/Icon';
import Plus from './components/base/Plus';

const PlusOutlined = props => (
  <Icon {...props} component={Plus} />
);

export default PlusOutlined
;
