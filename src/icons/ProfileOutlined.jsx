import React from 'react';
import Icon from '../components/Icon';
import Profile from './components/base/Profile';

const ProfileOutlined = props => (
  <Icon {...props} component={Profile} />
);

export default ProfileOutlined
;
