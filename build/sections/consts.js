const prefixCls = 'crm';

const mockUrlConfig = {
  port: 4000,
  host: 'localhost'
};

const envEnum = {
  static: 'static',
  development: 'development',
  production: 'production',
  test: 'test'
}

export {
  prefixCls,
  mockUrlConfig,
  envEnum
}
